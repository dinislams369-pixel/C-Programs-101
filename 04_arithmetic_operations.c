#include <stdio.h>
int main (){
    float X,Y ;  // using float data type to sore decimal values 
         printf("Enter valu of X and Y:");
          scanf("%f %f",&X,&Y);
            printf("Arithmetic Operations :\n");
          printf("   Addition(+) =%.1f\n",X+Y);
       printf("   Substraction (-) =%.1f\n",X-Y);
     printf("   Multiplication (*) =%.1f\n",X*Y);
   printf("   Division(/) =%.1f",X/Y);
return 0;
}
